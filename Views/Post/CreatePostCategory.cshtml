@model PortalMaggieCard.Models.PostCategoryView

@{
    ViewData["Title"] = "CreatePostCategory";
}
@if (TempData["Success"] != null)
{
    <div class="alert alert-success">
        <p><strong>Success:</strong> @TempData["Success"].ToString()</p>
    </div>
}
@if (TempData["Fail"] != null)
{
    <div class="alert alert-danger">
        <p><strong>Fail:</strong> @TempData["Fail"].ToString()</p>
    </div>
}

<h2>Create Post Category</h2>

<h4>Post Category View</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreatePostCategory">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PostCategoryName" class="control-label"></label>
                <input asp-for="PostCategoryName" class="form-control" />
                <span asp-validation-for="PostCategoryName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
